weapsy.admin.createPage = weapsy.admin.createPage || {};

weapsy.admin.createPage = (function ($) {
    return {
        loading: function () {
            weapsy.utils.showLoading("Creating Page");
        }
    }
}(jQuery));